type Login = {
    username: string;
    password: string;
}


type Login2 = {
    username2: string;
    password2: string;
}

type otp = {
    cd: string
}

type Addition = {
  courseCode: string;
};

type IdentityT = {
  examCode: string;
  candidateNumber: string;
};

type Additional = {
  schoolNumber: string;
  resumptionDate: string;
  entryCode: string;
};